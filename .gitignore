# Dependencies
node_modules/

# Clasp auth (contains OAuth tokens — never commit)
.clasprc.json
~/.clasprc.json

# Environment / secrets
.env
.env.*

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
*.code-workspace

# Claude Code — personal settings only; .claude/settings.json (shared/team
# config) is fine to commit if you add one later
.claude/settings.local.json

# Logs
*.log
npm-debug.log*

# Build / temp
dist/
tmp/
